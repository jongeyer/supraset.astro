---
import '../styles/markdown.css'

interface Props {
  class?: string;
  mixable?: boolean;
}
const { class: className, mixable } = Astro.props;
---

<article class="md-content max-w-prose mx-auto" class:list={[className, mixable ? 'mixable' : '']}>
  <slot />
</article>